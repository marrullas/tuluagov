import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:tuluagov/app/modules/convivencia/convivencia_page.dart';

main() {
  testWidgets('ConvivenciaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ConvivenciaPage(title: 'Convivencia')));
    //  final titleFinder = find.text('Convivencia');
    //  expect(titleFinder, findsOneWidget);
  });
}
