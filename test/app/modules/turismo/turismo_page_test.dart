import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:tuluagov/app/modules/turismo/turismo_page.dart';

main() {
  testWidgets('TurismoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TurismoPage(title: 'Turismo')));
    //  final titleFinder = find.text('Turismo');
    //  expect(titleFinder, findsOneWidget);
  });
}
