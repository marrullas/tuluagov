import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:tuluagov/app/modules/solidaridad/pages/solicitaayuda/solicitaayuda_page.dart';

main() {
  testWidgets('SolicitaayudaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SolicitaayudaPage(title: 'Solicitaayuda')));
    //  final titleFinder = find.text('Solicitaayuda');
    //  expect(titleFinder, findsOneWidget);
  });
}
