import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:tuluagov/app/modules/news/news_page.dart';

main() {
  testWidgets('NewsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NewsPage(title: 'News')));
    //  final titleFinder = find.text('News');
    //  expect(titleFinder, findsOneWidget);
  });
}
